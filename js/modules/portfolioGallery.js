// modules/portfolioGallery.js

export function initPortfolioGallery() {
    // Remova ou comente este bloco INTEIRO para desativar o ScrollReveal para os cards de portfólio
    /*
    if (typeof ScrollReveal !== 'undefined') {
        // Inicializa o ScrollReveal para os itens de portfólio
        ScrollReveal().reveal('.portfolio-card.sr-item', {
            distance: '50px',
            origin: 'bottom',
            opacity: 0,
            duration: 900,
            delay: 100,
            interval: 150,
            easing: 'ease-in-out',
            reset: false
        });
        console.log('ScrollReveal inicializado para cards de portfólio.');
    } else {
        console.warn('ScrollReveal não está carregado. As animações dos cards de portfólio não serão aplicadas.');
    }
    */

    // Você pode adicionar outras funcionalidades de galeria aqui no futuro,
    // como filtragem de categorias, lazy loading de imagens, etc.
}